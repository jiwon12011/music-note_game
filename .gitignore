node_modules/
package-lock.json
.DS_Store
Thumbs.db

# 개발용 렌더 스크린샷 (도구 스크립트는 유지)
tools/*.png

# 고해상도 원본 (assets/*.webp 변환본만 커밋)
/image *.png
/lp_with_characters_fixed.png

# 게임 캐릭터/배경 원본 PNG (~530MB) — play/public/img/*.webp 변환본만 커밋
/무제 폴더/

# 디자인 원본/참고 export (배포·git 제외) — assets/*.webp 변환본만 사용
/IMG/
/EX/
